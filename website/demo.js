            function colors(color) {
                    document.body.className = color;
            }
            function navbar(top) {
            	$(".page").removeClass().addClass(top);
            }
            function sidebar(position) {
                $(".sidebar").removeClass().addClass(position);
            }